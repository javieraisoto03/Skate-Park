<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Panel de administración</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/estilos.css">
</head>
<body>
  <div class="container py-4">
    <h1>Panel de administración</h1>
    <hr>
    <h2>Lista de participantes</h2>
    <table class="table table-dark">
      <thead>
        <tr>
          <th>#</th>
          <th>Foto</th>
          <th>Nombre</th>
          <th>Años de experiencia</th>
          <th>Especialidad</th>
          <th>Email</th>
          <th>Estado</th>
          <th>Aprobar</th>
        </tr>
      </thead>
      <tbody>
        {{#each skaters}}
        <tr>
          <th scope="row">{{@index}}</th>
          <td><div style="background-image: url('/images/{{foto}}');"></div></td>
          <td>{{nombre}}</td>
          <td>{{anos_experiencia}}</td>
          <td>{{especialidad}}</td>
          <td>{{email}}</td>
          <td>{{#if estado}}Aprobado{{else}}En revisión{{/if}}</td>
          <td>
            <form method="POST" action="/admin/{{id}}?_method=PUT">
              <button type="submit" class="btn {{#if estado}}btn-danger{{else}}btn-success{{/if}}">
                {{#if estado}}Desaprobar{{else}}Aprobar{{/if}}
              </button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>
